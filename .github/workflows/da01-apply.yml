name: Apply DA01 foundation

on:
  push:
    branches:
      - 1.10.0
    paths:
      - .github/workflows/da01-apply.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: 1.10.0

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: |
            8.0.x
            9.0.x
            10.0.x

      - name: Apply DA01 coordinated updates
        run: python3 .github/scripts/da01-apply.py

      - name: Build DA01
        run: dotnet build Icod.TermInfo.sln -c Debug

      - name: Test DA01
        run: dotnet test Icod.TermInfo.sln -c Debug --no-build

      - name: Commit DA01
        run: |
          rm .github/scripts/da01-apply.py
          rm .github/workflows/da01-apply.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Implement DA01 database-set foundation"
          git push origin HEAD:1.10.0
