# Icod.TermInfo 1.14.0 RB08 — Alpha-8 Qualification Candidate

RB08 release hardening is complete on the immediately preceding closure foundation head:

```text
a8a76a0f41ba06fae00d94c4df65f7dfa46813ad
```

That tree:

- preserves the user-added `.github/FUNDING.yml` metadata;
- freezes the complete 106-type `Icod.TermInfo.Inspection` surface at normalized-LF SHA-256 `e9f240a562aec5274d64fb2ec3647862fe4ef5684582af2b442ba3c55e189497`;
- freezes JSON schemas v1 through v6, including v6 SHA-256 `9d51ec6659f8978c867408881eefb106f3c4976dfcf2da12b23c374a222665c2`;
- verifies the whole 1.14 surface before subtracting the reviewed RB01/RB02/RB03 type additions and RB06 renderer-member additions to reconstruct exact frozen 1.13;
- retains the established historical Inspection reconstruction through frozen 1.10;
- records the raster-backend selection guide, exact freeze fingerprints, and release audit; and
- leaves production `Icod.TermInfo.Inspection` free of `Icod.Terminal`, with RB07 qualification pinned to published `Icod.Terminal 1.13.0`.

This user-authored checkpoint exists only to trigger the authoritative PR qualification matrix. RB08 / Alpha-8 is not accepted until this exact head passes all 12 jobs.
