# Icod.TermInfo 1.14.0 RB05 — Runtime-Integration Candidate Composition

## Scope

RB05 removes caller-side profile-packaging boilerplate between the frozen 1.13 runtime-integration result and the 1.14 raster-backend candidate model. It adds no new public type and does not change 1.13 integration, RB03 candidate evaluation, or RB04 selection semantics.

## Accepted baseline

RB05 begins from accepted RB04 / `1.14.0-Alpha-4` exact head:

```text
d202fd2e452b083de92b84521f1845a30354aede
```

qualified by workflow #896 / run `34877834128` with all 12 jobs green.

## TDD RED witness

The RB05 tests-only RED head is:

```text
f13fa4e487567a94b1edf2849b5b21a10e81b84e
```

Workflow #897 / run `34878847456` produced the intended RED result. The observed macOS Build failed with **21 errors and 0 warnings**, all `CS7036` at the seven new constructor call sites across net8/net9/net10 because `RasterBackendCandidate` exposed only its existing three-profile constructor. No unrelated compile, analyzer, convention, or frozen-contract failure was present.

## Composition API

RB05 adds exactly one reviewed public constructor to the RB01-added `RasterBackendCandidate` type:

```csharp
public RasterBackendCandidate(
    RasterBackendProfile backendProfile,
    PersistentRasterRuntimeIntegrationResult integration
)
```

The constructor requires both arguments, retains the supplied backend profile unchanged, and takes the exact `LifecycleProfile` and `PlacementProfile` references already retained by the frozen 1.13 integration result.

The exact public-api-snapshot manifest line is recorded in:

```text
docs/1.14.0-RB05-INSPECTION-PUBLIC-API-ADDITIVE-MEMBERS.txt
```

Because `RasterBackendCandidate` itself is already wholly removed by the RB01 reviewed-type ledger during 1.14 -> 1.13 compatibility reconstruction, RB05 does not change the cumulative reviewed-type count of 16 and does not change the frozen 1.13 fingerprint.

## Trust and orchestration boundary

The composition constructor deliberately performs no semantic work beyond packaging existing profiles. It does not:

- re-integrate runtime observations;
- manufacture or reclassify evidence;
- assign source ordinals;
- inspect inconclusive observations;
- infer backend availability from lifecycle or placement observations;
- require `PersistentRasterRuntimeIntegrationResult.Succeeded` to be true; or
- merge runtime contexts belonging to different backends.

A failed or partially successful integration result can therefore still be packaged. The candidate receives exactly the profiles represented by that integration result, including any unchanged family preserved by 1.13 after an integration limitation. Backend availability remains the separately supplied `RasterBackendProfile`.

Consumers verifying multiple backends continue to maintain separate 1.13 integration contexts. The new constructor only removes the final manual extraction of each context's lifecycle and placement profiles.

## Behavioral equivalence

Tests prove that constructing a candidate from `(backendProfile, integration)` is behaviorally equivalent to constructing it manually from `(backendProfile, integration.LifecycleProfile, integration.PlacementProfile)`. Existing `RasterBackendPlanner.Evaluate(...)` and `Plan(...)` outputs therefore remain authoritative and unchanged.

## Behavioral GREEN checkpoint

Minimum production implementation commit:

```text
28a0f7037434057cb79387db056639f8b913b6b8
```

Workflow #898 / run `34879032335` completed `success` with all 12 jobs green: Windows/Linux/macOS Build+Test, Windows PowerShell compatibility reconstruction, coordinated Staging package verification and isolated consumer, installed-tool smokes on all three operating systems, and all six archive RID smokes.

This is the RB05 behavioral GREEN boundary before Alpha-5 accounting/version synchronization.

## Version

RB05 advanced coordinated development to `1.14.0-Alpha-5`. RB04 remains frozen as the accepted Alpha-4 checkpoint above.

## Qualification

The synchronized Alpha-5 accounting candidate was:

```text
44d2fa154cc4d02c2b654e4e7f38b4f573bb81a1
```

Workflow #899 / run `34880035690` completed `success` with all 12 jobs green.

The final accepted Alpha-5 exact head is:

```text
a4aab5e7af4d554cad7c978cef20491744100c80
```

Workflow #900 / run `34880872852` completed `success` with all 12 jobs green. RB05 / Alpha-5 is therefore frozen at that exact head and run.
