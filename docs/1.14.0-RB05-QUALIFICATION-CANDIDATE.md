# Icod.TermInfo 1.14.0 RB05 — Alpha-5 Qualification Candidate

RB05 runtime-integration composition behavior was established at exact head:

```text
28a0f7037434057cb79387db056639f8b913b6b8
```

Workflow #898 / run `34879032335` completed `success` with all 12 jobs green.

Alpha-5 accounting and version authority were then synchronized atomically at exact head:

```text
44d2fa154cc4d02c2b654e4e7f38b4f573bb81a1
```

Workflow #899 / run `34880035690` completed `success` with all 12 jobs green. That tree:

- advances the coordinated version to `1.14.0-Alpha-5`;
- records the exact public-api-snapshot constructor signature added to `RasterBackendCandidate`;
- freezes RB04 / Alpha-4 at accepted head `d202fd2e452b083de92b84521f1845a30354aede`, workflow #896 / run `34877834128`;
- records the RB05 RED and behavioral GREEN witnesses;
- preserves the cumulative 16 reviewed 1.14 `RasterBackend*` public types because the new constructor belongs to the already-RB01-added `RasterBackendCandidate` type;
- preserves exact frozen 1.13 reconstruction and the historical compatibility chain; and
- changes no production behavior beyond the already-qualified two-argument candidate-composition constructor.

This documentation-only checkpoint triggers authoritative final PR qualification. RB05 is accepted only if the exact head containing this record passes the full 12-job Windows/Linux/macOS, compatibility, package, installed-tool, and archive matrix.
