# Icod.TermInfo 1.14.0 RB04 — Alpha-4 Qualification Candidate

RB04 deterministic preference-aware backend selection is behaviorally complete and Alpha-4 accounting has been synchronized.

## Behavioral boundary

The tests-only RED head is:

```text
4410808a1626dbdb6a21ca2be3bf78b344f10255
```

Workflow #887 / run `34872792326` failed as intended only because `RasterBackendPlanner.Plan(...)` did not yet exist.

The minimum GREEN production implementation is:

```text
e6e276b6d4b86e495733228571e13f77385e00f7
```

Workflow #888 / run `34874959660` passed all 12 jobs and established the RB04 behavioral GREEN boundary.

## Alpha-4 accounting candidate

The synchronized Alpha-4 accounting tree, including the repository-convention correction in the new authority test, is:

```text
855f6209fa416b6d0f8f20444be174ae7ba8f0f3
```

Workflow #895 / run `34877122811` passed all 12 jobs:

- Windows, Linux, and macOS Build+Test;
- Windows PowerShell current -> exact frozen 1.13 -> historical compatibility reconstruction;
- coordinated Staging pack and exact package verification;
- isolated package consumer;
- installed-tool package smokes on Windows, Linux, and macOS; and
- all six archive RID smokes.

The immediately preceding accounting run #894 demonstrated that production remained build-clean but exposed one repository parenthesis-spacing violation in the newly added `RB04VersionAuthorityTests.cs`. The only correction was `StartsWith( '#')` -> `StartsWith( '#' )`; no production assembly, selection semantics, public API, compatibility rule, package topology, or schema changed.

## Public API accounting

RB04 adds exactly one reviewed member to the RB03-added `RasterBackendPlanner` type:

```csharp
public static RasterBackendSelectionPlan Plan(
    IEnumerable<RasterBackendCandidate> candidates,
    RasterBackendSelectionRequest request,
    RasterBackendSelectionOptions? options = null
)
```

Its exact public-api-snapshot manifest signature is recorded in:

```text
docs/1.14.0-RB04-INSPECTION-PUBLIC-API-ADDITIVE-MEMBERS.txt
```

The ledger line was independently checked against the public API snapshot produced by workflow #888. Because `RasterBackendPlanner` is already wholly removed by the RB03 reviewed-type ledger during 1.14 -> 1.13 reconstruction, the cumulative reviewed 1.14 public-type count remains exactly 16 and the frozen 1.13 fingerprint and historical reconstruction chain remain unchanged.

## Preserved authorities

RB03 remains frozen as accepted `1.14.0-Alpha-3` on exact head:

```text
22f38e9c6516bec9d5f6b8662f00e9b5bace7c2b
```

qualified by workflow #885 / run `34871582203`.

The live coordinated version is now `1.14.0-Alpha-4`.

## Final qualification gate

This documentation checkpoint triggers authoritative PR qualification on the exact Alpha-4 tree containing this record. RB04 is not accepted until that exact head passes the complete 12-job qualification matrix. Once accepted, RB04 is frozen and development proceeds to RB05 / Alpha-5 runtime-integration composition and orchestration.
