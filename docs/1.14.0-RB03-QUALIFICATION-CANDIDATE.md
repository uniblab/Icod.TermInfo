# Icod.TermInfo 1.14.0 RB03 — Alpha-3 Qualification Candidate

RB03 release accounting was synchronized by exact-text guarded commit:

```text
9e777e8439672862d80f2a7ddb5fda6daf685e38
```

That synchronized tree:

- advances the coordinated version to `1.14.0-Alpha-3`;
- records `RasterBackendPlanner` as the sole RB03 public API addition;
- reconstructs frozen 1.13 by subtracting the distinct RB01, RB02, and RB03 ledgers, totaling exactly 16 reviewed 1.14 `RasterBackend*` public types;
- preserves the historical compatibility chain through frozen 1.10;
- advances frozen 1.11/1.12/1.13 surface tests to subtract all three reviewed 1.14 ledgers; and
- freezes RB02 as the accepted Alpha-2 checkpoint.

Initial exact-head qualification on `6dcbec2570882e8e6c7139185688a9b73b8d6713` proved Windows/macOS/Linux Build and ordinary tests green, including 579 Inspection tests per TFM, but exposed one compatibility-helper cardinality defect: PowerShell enumerated the `HashSet` returned by `Read-ApprovedTypes`, so the one-entry RB03 ledger collapsed to a scalar and strict-mode `.Count` failed in both Windows compatibility verification and Linux package verification.

The generic correction is commit:

```text
bce30fddf950e850b8a699219c1c503fa88b1489
```

`Read-ApprovedTypes` now uses `Write-Output -NoEnumerate` so tranche ledgers retain `HashSet` identity for any cardinality. This changes no production assembly, public API, planner semantics, frozen hash, package topology, or schema.

This user-authored documentation checkpoint triggers authoritative PR qualification on the corrected Alpha-3 tree. RB03 is not accepted until that exact qualification head passes all 12 jobs.
