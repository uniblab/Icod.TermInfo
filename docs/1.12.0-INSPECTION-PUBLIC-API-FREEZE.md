# Icod.TermInfo 1.12.0 Inspection Public API Freeze

## Exact surface identity

The complete `Icod.TermInfo.Inspection` 1.12 public reflection manifest is frozen by normalized-LF SHA-256:

```text
f71501dcd27a530051c1a02083325144ced2b6173b6b967b9571c620815198f0
```

The manifest uses `Icod.TermInfo.PublicApiSnapshot/v1`, retains assembly version `1.0.0.0`, and contains exactly 81 exported public types. The fingerprint was taken from the exact Alpha-7 Staging package/API artifact that passed workflow #709 / run `34761637133` before PG08 changed release metadata.

PG08 release verification generates the complete manifest from each candidate assembly and rejects any normalized fingerprint other than the value above.

## Compatibility decomposition

The 1.12 freeze is composite and additive above the frozen 1.11 surface:

1. the complete 1.11 Inspection manifest remains frozen by normalized-LF SHA-256 `69c7350d5d44d502ecf1698c8fe1c1336f03d38eb1a36e36219f50ac33585a86`;
2. the exact approved 1.12 `PersistentRasterPlacement*` type blocks are listed in `docs/1.12.0-PG01-INSPECTION-PUBLIC-API-ADDITIONS.txt`;
3. the exact additive `TermInfoJsonRenderer` version-4 member lines are listed in `docs/1.12.0-PG06-INSPECTION-PUBLIC-API-ADDITIVE-MEMBERS.txt`; and
4. the complete resulting 1.12 manifest is independently locked by the SHA-256 above.

Release verification therefore proves both directions:

- removing exactly the reviewed 1.12 type/member delta reconstructs the frozen 1.11 manifest fingerprint; and
- the complete current 1.12 manifest matches the frozen whole-surface fingerprint.

An accidental change to a historical member or to one of the reviewed 1.12 additions fails the release gate.

## Cross-target equality

The package continues to target:

```text
net8.0
net9.0
net10.0
```

Release package verification requires byte-equivalent public API manifests across all three target frameworks. No target framework owns a divergent 1.12 API surface.

## Lower-layer contracts

Version 1.12 does not intentionally change the frozen Runtime, Source, Compiler, or Termcap public APIs. Their existing release baselines and assembly identity `1.0.0.0` remain authoritative.

`Icod.TermInfo.Inspection` also retains its production dependency boundary: it does not acquire an `Icod.Terminal` dependency. Terminal interoperability is qualified only in the dedicated PG07 downstream consumer and sample.

## Machine-readable contracts

The public API freeze is independent from the JSON schema freeze. Exact normalized-LF fingerprints for schema versions 1 through 4 are recorded in `docs/1.12.0-PG08-FREEZE-FINGERPRINTS.txt`.

Versions 1, 2, and 3 remain immutable historical contracts. Version 4 contains exactly the two 1.12 placement document kinds:

```text
persistentRasterPlacementProfile
persistentRasterPlacementPlan
```
