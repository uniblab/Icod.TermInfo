# Icod.TermInfo 1.11.0 RL08 — Release Hardening and Freeze

## Status

RL08 is implemented and validated as `1.11.0-Alpha-8`.

**Validated head:** `37393dcf7bbf75f8914cc518152052852591c6e6`  
**Workflow:** pull-request #286 / run `34662961312`

The validated workflow passed Windows, Linux, and macOS build/test, exact Staging package/API verification, isolated package consumption, installed-tool smoke on all three hosts, and all six tool-suite archive smoke jobs.

The Linux Staging build completed with `0 Warning(s)` and `0 Error(s)`.

## Release-hardening objective

RL08 adds no new lifecycle feature semantics. It freezes and hardens the surface produced by RL01-RL07, completes consumer/release documentation, and establishes the promotion boundary between the validated Alpha-8 candidate and stable `1.11.0`.

Stable promotion is permitted only as a release-identity/documentation transition. It may not add or change public API, schema fields, lifecycle semantics, package dependencies, target frameworks, command behavior, or archive RIDs.

## Exact Inspection API freeze

The exact complete 1.11 `Icod.TermInfo.Inspection` public reflection manifest has normalized SHA-256:

```text
69c7350d5d44d502ecf1698c8fe1c1336f03d38eb1a36e36219f50ac33585a86
```

It contains exactly 67 exported public types.

Release verification independently proves all of the following:

1. the complete current 1.11 manifest matches that exact fingerprint;
2. public API is identical across `net8.0`, `net9.0`, and `net10.0`;
3. after removing exactly the 16 reviewed 1.11 lifecycle type blocks and 6 reviewed additive `TermInfoJsonRenderer` members, the remaining manifest equals the frozen 1.10 baseline byte-for-byte.

The reviewed delta remains recorded in:

```text
docs/1.11.0-INSPECTION-PUBLIC-API-ADDITIONS.txt
docs/1.11.0-INSPECTION-PUBLIC-API-ADDITIVE-MEMBERS.txt
```

The complete composite freeze is documented in:

```text
docs/1.11.0-INSPECTION-PUBLIC-API-FREEZE.md
```

Runtime, Source, Compiler, and Termcap retain their previously frozen public APIs and assembly version `1.0.0.0`.

## JSON schema freeze

All three machine-readable schema contracts are fingerprinted and package-verified:

```text
version 1  76578f421b254802d24453af6868edaf8c23c4b78a87c7e8ef86b233ff0e8500
version 2  ae4d53608881344e902f02303c71e2d432500969e60cfb005d70feea607499d0
version 3  33ca95aee120f84d0d160ac189f8ddb4db183361b7bd83885c99c1c8ed355a97
```

Versions 1 and 2 remain immutable historical contracts. Version 3 contains exactly the two 1.11 lifecycle document kinds:

```text
persistentRasterLifecycleProfile
persistentRasterLifecyclePlan
```

The exact fingerprints are recorded in `docs/1.11.0-RL08-FREEZE-FINGERPRINTS.txt`.

## Hardening retained from RL02-RL06

The permanent lifecycle suites retain coverage for:

- invalid enum values and constructor argument validation;
- evidence/source/provenance bounds;
- evidence-count and plan-size bounds;
- immutable snapshots and duplicate evidence;
- deterministic ordinal ordering;
- `tr-TR` / invariant-culture equivalence;
- explicit evidence precedence and contradiction retention;
- unsupported versus unknown versus contradicted planning outcomes;
- cancellation boundaries;
- JSON UTF-8 output bounds;
- repeated rendering and culture independence; and
- exact version-3 schema/package structure.

No release-closure change weakens those gates.

## Downstream and distribution qualification

The Alpha-8 package gate re-runs the RL07 package-only lifecycle consumer on:

```text
net8.0
net9.0
net10.0
```

Workflow #286 records:

```text
RL07 package-only lifecycle consumer passed on net8.0, net9.0, and net10.0 for 1.11.0-Alpha-8.
```

The checked-in `samples/Icod.TermInfo.PersistentRasterLifecycle.Sample` also executes on all three reusable TFMs and emits the reviewed version-3 profile and plan documents.

The distribution fan-out passed:

```text
installed Icod.TermInfo.Tools: Windows, Linux, macOS
archives: win-x64, win-arm64, linux-x64, linux-arm64, osx-x64, osx-arm64
```

Existing DA07 installed-tool and archive automation remains green and unchanged.

## Documentation and metadata closure

RL08 updates the release-facing contract without changing product semantics:

- root `README.md` identifies stable `1.10.0` and the `1.11.0-Alpha-8` candidate correctly;
- stable install examples use `1.10.0` until stable 1.11 promotion;
- `Icod.TermInfo.Inspection/README.md` documents the lifecycle surface and ownership boundary;
- `docs/VERSIONING.md` defines the 1.11 release line and promotion-only rule;
- `docs/COMPATIBILITY.md` defines the 1.11 compatibility freeze;
- `docs/1.11.0-PERSISTENT-RASTER-LIFECYCLE-GUIDE.md` provides the consumer guide;
- coordinated NuGet/tool release notes describe 1.11 accurately; and
- `docs/1.11.0-RELEASE-AUDIT.md` records the release closure and validation evidence.

## T45 stale-current-version correction

The first documentation-complete Alpha-8 candidate exposed one historical completion-gate defect rather than a product regression.

Workflow #285 / run `34659702679` failed only `T45CompletionGateTests.FinalReadmeUsesStablePackageVersionAndPolicies` across the Runtime TFMs. The README correctly identified `1.10.0` as the current stable release, while T45 still hard-coded six `1.9.0` stable install/tool strings.

The test was corrected to require the actual current stable `1.10.0` commands. No product code, public API, schema, package dependency, or lifecycle semantic changed.

Exact corrected head `37393dcf7bbf75f8914cc518152052852591c6e6` then passed workflow #286 completely.

## Style contract

C# follows 1TBS. Every control-flow body is enclosed in explicit `{ ... }` braces; unbraced one-line clauses are not permitted. Normal `} else if ( ... ) {` form is permitted.

## Gate result

RL08 satisfies its roadmap gate. `1.11.0-Alpha-8` is the frozen release candidate for the persistent-raster lifecycle feature line.

The only permitted next product transition is promotion to stable `1.11.0` through coordinated release identity/documentation metadata. That stable head must receive a fresh full Staging validation before merge or tagging.

No merge, tag, registry publication, or release action is performed by this freeze.